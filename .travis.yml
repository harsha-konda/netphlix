sudo: true
language: node_js
addons:
  apt:
    packages:
      - "python"
      - "python-pip"
node_js:
  - "6"
install:
  - sudo pip install flask
  - sudo pip install requests
  - npm install -g @angular/cli
  - npm install
script:
  - npm run build
