kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: es-data-vc
  annotations:
    volume.alpha.kubernetes.io/storage-class: default
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 10Gi  
